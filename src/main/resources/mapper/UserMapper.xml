<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jc.aiagent.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.jc.aiagent.entity.User">
        <id property="id" column="id"/>
        <result property="email" column="email"/>
        <result property="password" column="password"/>
        <result property="nickname" column="nickname"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="selectByEmail" resultMap="UserResultMap">
        SELECT id, email, password, nickname, status, create_time, update_time
        FROM user
        WHERE email = #{email}
        LIMIT 1
    </select>

    <select id="selectById" resultMap="UserResultMap">
        SELECT id, email, password, nickname, status, create_time, update_time
        FROM user
        WHERE id = #{id}
        LIMIT 1
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (email, password, nickname, status, create_time, update_time)
        VALUES (#{email}, #{password}, #{nickname}, #{status}, #{createTime}, #{updateTime})
    </insert>

    <select id="countByEmail" resultType="int">
        SELECT COUNT(*) FROM user WHERE email = #{email}
    </select>

</mapper>
