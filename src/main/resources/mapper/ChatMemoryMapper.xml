<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jc.aiagent.mapper.ChatMemoryMapper">
    <resultMap id="ChatMemoryResultMap" type="com.jc.aiagent.entity.ChatMemoryEntity">
        <id property="id" column="id"/>
        <result property="chatId" column="chat_id"/>
        <result property="userId" column="user_id"/>
        <result property="content" column="content"/>
        <result property="type" column="type"/>
        <result property="timeStamp" column="timestamp"/>
    </resultMap>

    <select id="selectByChatId" resultMap="ChatMemoryResultMap">
        SELECT id, chat_id, user_id, content, type, `timestamp`
        FROM chat_memory
        WHERE chat_id = #{chatId}
          AND user_id = #{userId}
        ORDER BY `timestamp` ASC
    </select>

    <insert id="batchInsert">
        INSERT INTO chat_memory (chat_id, user_id, content, type, `timestamp`)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.chatId}, #{item.userId}, #{item.content}, #{item.type}, #{item.timeStamp})
        </foreach>
    </insert>

    <delete id="deleteByChatId">
        DELETE FROM chat_memory WHERE chat_id = #{chatId} AND user_id = #{userId}
    </delete>
</mapper>
